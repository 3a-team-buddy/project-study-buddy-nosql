import React from "react";
import { Button } from "@/components/ui";

export const SessionEditBtn = () => {
  return (
    <Button
      variant={"secondary"}
      className="w-1/2 text-accent-foreground cursor-pointer"
    >
      Edit
    </Button>
  );
};
